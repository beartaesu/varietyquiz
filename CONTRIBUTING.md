# 기여 가이드

이 프로젝트에 기여해주셔서 감사합니다! 🎉

## 개발 환경 설정

1. 프로젝트 포크 및 클론
```bash
git clone https://github.com/your-username/varietyquiz.git
cd varietyquiz
```

2. 의존성 설치
```bash
npm install
```

3. 환경 변수 설정
```bash
cp .env.example .env
# .env 파일 수정
```

4. 개발 서버 실행
```bash
npm run dev
```

## 브랜치 전략

- `main`: 프로덕션 브랜치
- `develop`: 개발 브랜치
- `feature/*`: 새 기능 개발
- `fix/*`: 버그 수정
- `docs/*`: 문서 수정

## 커밋 메시지 규칙

```
<type>: <subject>

<body>
```

### Type
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `docs`: 문서 수정
- `style`: 코드 포맷팅
- `refactor`: 코드 리팩토링
- `test`: 테스트 추가
- `chore`: 빌드 설정 등

### 예시
```
feat: 배드민턴 팀 매칭 알고리즘 개선

- 이전 매칭 히스토리 고려
- 실력 균형 가중치 조정
```

## Pull Request 프로세스

1. 새 브랜치 생성
```bash
git checkout -b feature/your-feature
```

2. 변경사항 커밋
```bash
git add .
git commit -m "feat: your feature"
```

3. 푸시
```bash
git push origin feature/your-feature
```

4. GitHub에서 Pull Request 생성

5. 코드 리뷰 대기

## 코드 스타일

- TypeScript strict mode 사용
- ESLint 규칙 준수
- Prettier로 포맷팅
- 의미있는 변수명 사용
- 주석은 필요한 경우에만

## 테스트

현재 테스트는 구현되어 있지 않습니다. 테스트 추가는 환영합니다!

## 문의

질문이 있으시면 Issue를 생성해주세요.
